# Common editor / OS ignores
.DS_Store
Thumbs.db
desktop.ini

# Local agent documentation (keep AGENTS.md local)
AGENTS.md

# IDEs
.idea/
.vscode/
*.iml

# Build / output
/build/
/out/
/target/
/bin/
/gen/
/.gradle/

# Java / Kotlin
*.class
*.log
*.tmp
*.bak

# If you need to ignore specific JARs locally, prefer .git/info/exclude
# This project intentionally contains binary jars in the repo root; avoid using
# a blanket `*.jar` pattern unless you want to ignore all untracked jars.

# Configuration / secrets
.env
.env.*
credentials.properties
secrets.properties

# Misc
tags
*.swp
*~
